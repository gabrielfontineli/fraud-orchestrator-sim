{
  "name": "fraud-orchestrator-sim",
  "version": "0.1.0",
  "private": true,
  "description": "Fraud orchestrator simulator (Node/Express)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "jest --runInBand",
    "lint": "eslint ."
  },
  "dependencies": {
    "express": "^4.19.2",
    "@aws-sdk/client-dynamodb": "^3.645.0"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "jest": "^29.7.0",
    "supertest": "^7.0.0",
    "nodemon": "^3.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}